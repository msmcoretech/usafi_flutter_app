import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const primary = Color(0xFF6447CE);
  static const secondary = Color(0xFFFFFFFF);

  // Backgrounds
  static const scaffoldBg = Color(0xFF6447CE);
  static const background = Color(0xFFF6F6F6);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF505050);
  static const textHint = Color(0xFFB0B0B0);

  // Buttons
  static const buttonPrimary = primary;
  static const buttonDisabled = Color(0xFFCCCCCC);

  // Borders / Divider
  static const border = Color(0xFFB2B2B2);
  static const borderColor = Color(0xFFC4C4C4);
  static const divider = Color(0xFFD9D9D9);

  // Error / Success
  static const error = Color(0xFFFF3B30);
  static const success = Color(0xFF33BF80);

  // Other
 static const yellow = Color(0xFFF8EE4D);
 static const green = Color(0xFF33BF80);
 static final lightPrimary = const Color(0xFF775BDE).withOpacity(0.1);
 static final circleButton = const Color(0xFF775BDE);
 static final mediumPrimary = const Color(0xFF775BDE).withOpacity(0.2);
 static final jobHeader = const Color(0xFFE6E3F3);
 static final jobPending = const Color(0xFFC5B3FF);
 static final jobRejected = const Color(0xFFEB4335);
 static final jobFinished = const Color(0xFF000000).withOpacity(0.2);
}
